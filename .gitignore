node_modules/
dist/
data/
*.db
.env
.env.local
*.log
*.tar.gz
.DS_Store
Thumbs.db
# Runtime config override (per-deployment). Defaults live in config/pharos.default.yaml.
pharos.yaml
# Pre-deploy backup snapshots
.pharos-pre-deploy-backup/
# Cross-session handoff files (transient)
SESSION-HANDOFF.md
AGENT-*-HANDOFF.md
# Local Claude Code state (skills, settings) — not project code
.claude/skills/
